node_modules/
dist/
*.db
.env

# Runtime config with real credentials (generated by setup wizard)
twilight-bots.config.json

# Compiled relayer-cli binary
bin/

# Cloned relayer source (used during setup to build binary)
.relayer-src/

# Claude Code per-machine state (lock files, locally-installed skills)
.claude/scheduled_tasks.lock
.claude/skills/

